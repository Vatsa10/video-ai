# Local GPU embedding + retrieval
torch                # install the CUDA build matching your GPU (see README)
sentence-transformers  # CLIP image+text embeddings
qdrant-edge-py==0.7.2  # in-process hybrid vector shard (folder, no server)
pillow
numpy
# Cloud reasoning (OpenAI SDK also drives Kimi/Moonshot via base_url)
openai
python-dotenv
# Web UI
gradio
# Object-level memory (VIDEOQA_OBJECTS=1) — heavy (YOLOE + a 572MB text encoder), GPU
# recommended. Comment this block out to deploy the lighter frame-only pipeline.
ultralytics
lap
opencv-python
clip-anytorch        # YOLOE open-vocab text encoder (provides the `clip` module)
